<section class="section bg-cream center">
  <div class="container">
    <img class="enso" src="{{ 'logo.svg' | asset_url }}" width="64" height="64" alt="" style="margin:0 auto 24px">
    <h1 class="display">This page steeped too long</h1>
    <p class="muted" style="margin:12px 0 26px">We couldn't find that page.</p>
    <a class="btn btn-primary" href="{{ routes.root_url }}">Back home</a>
    <a class="btn btn-outline" href="/products/matcha-sous" style="margin-left:8px">Shop the mixer</a>
  </div>
</section>
