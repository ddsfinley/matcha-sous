<section aria-label="{{ page.title }}">
  <div class="wrap">
    <div class="head head--idx"><div class="head-body"><h1 class="display">{{ page.title }}</h1></div></div>
    <div class="article">{{ page.content }}</div>
  </div>
</section>
{% schema %}{ "name": "Page" }{% endschema %}
